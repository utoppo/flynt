<div class="flyntComponent componentSpacing {{ options.theme }}" is="flynt-slider-images">
  <script type="application/json">{{ jsonData|json_encode }}</script>
  <div class="centerMaxWidthContainer">
    {% if preContentHtml %}
      <div class="preContent">
        {{ preContentHtml|e('wp_kses_post') }}
      </div>
    {% endif %}
    <div data-slider class="slider swiper-container themeLight">
      <div class="swiper-wrapper">
        {% for image in images %}
          <div class="slider-item swiper-slide">
            <figure class="figure">
              <img is="flynt-image" class="figure-image"
                src="{{ image.src|resizeDynamic(0, 540) }}"
                srcset="{{ placeholderImage(image.aspect * 540, 540) }}"
                data-src="{{ image.src|generateRiasUrl(null, 0)}}"
                alt="{{ image.alt|e }}">
              {% if image.caption %}
                <figcaption class="figure-caption">{{ image.caption|e }}</figcaption>
              {% endif %}
            </figure>
          </div>
        {% endfor %}
      </div>
      <button type="button" data-slider-button="prev" class="slider-button slider-button--prev">
        <i data-feather="arrow-left"></i>
      </button>
      <button type="button" data-slider-button="next" class="slider-button slider-button--next">
        <i data-feather="arrow-right"></i>
      </button>
    </div>
  </div>
</div>
